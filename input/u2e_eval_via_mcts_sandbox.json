{
  "name": "u2e_eval_via_mcts_sandbox",
  "description": "通过 MCTS 共享沙箱会话评估一组 patch（patch 模式），返回 ok/score/output_log 等字段，与 mcts_sandbox_via_session 完全兼容",
  "long_running": true,
  "module": "utils.u2e_sandbox_inputs",
  "function": "u2e_eval_via_mcts_sandbox",
  "async": true,
  "parameters": {
    "type": "object",
    "required": ["sandbox_in_ch", "ancestor_patches", "new_patches"],
    "properties": {
      "sandbox_in_ch": {
        "type": "string",
        "description": "共享沙箱会话输入通道 ID（由 start_sandbox_session 创建）"
      },
      "ancestor_patches": {
        "type": "array",
        "description": "祖先 patch 列表（已应用到沙箱基准状态的累积改动，patch 模式下通常为 []）"
      },
      "new_patches": {
        "type": "array",
        "description": "本次新增 patch 列表（U2E individual 的 patches 字段，含全量函数快照）"
      }
    }
  }
}
