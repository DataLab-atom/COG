{
  "name": "mcts_gate",
  "description": "MCTS Human Gate：打印当前搜索状态并阻塞等待外部通过 channel 注入搜索决策",
  "long_running": true,
  "module": "utils.mcts.inputs",
  "function": "mcts_gate",
  "async": true,
  "parameters": {
    "type": "object",
    "required": ["gate_channel_id", "generation", "tree_text", "new_nodes"],
    "properties": {
      "gate_channel_id": {
        "type": "string",
        "description": "预先创建的通道 ID，外部通过此 ID 推送决策"
      },
      "generation": {
        "type": "integer",
        "description": "当前代数（用于日志显示）"
      },
      "tree_text": {
        "type": "string",
        "description": "搜索树状态文本快照（显示给用户参考）"
      },
      "new_nodes": {
        "type": "array",
        "description": "本代新生成的子节点列表（显示 Top 候选）"
      }
    }
  }
}
