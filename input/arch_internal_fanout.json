{
  "name": "arch_internal_fanout",
  "description": "并发为每个 file 迭代调用 arch_internal_definer，处理 needs 递归，返回所有内部节点定义",
  "long_running": false,
  "module": "utils.arch_internal_fanout",
  "function": "arch_internal_fanout",
  "async": true,
  "parameters": {
    "type": "object",
    "required": ["files", "requirement", "global_ids"],
    "properties": {
      "files":       { "type": "array",  "description": "所有 file 定义列表（含叶子节点）" },
      "requirement": { "type": "string", "description": "原始需求描述" },
      "global_ids":  { "type": "array",  "description": "叶子阶段结束后的全局 id 列表" }
    }
  }
}
