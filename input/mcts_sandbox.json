{
  "name": "mcts_sandbox",
  "description": "MCTS 沙盒评估：在隔离临时目录中重放节点祖先链的全部 patch，运行 main.py 并解析 __METRICS__ 输出返回评分",
  "long_running": true,
  "module": "utils.mcts_inputs",
  "function": "mcts_sandbox",
  "async": true,
  "parameters": {
    "type": "object",
    "required": ["project_root", "ancestor_patches", "new_patch", "timeout"],
    "properties": {
      "project_root": {
        "type": "string",
        "description": "项目根目录绝对路径"
      },
      "ancestor_patches": {
        "type": "array",
        "description": "父节点累积的 patch 列表（按应用顺序），每项含 target_file/target_type/target_name/code"
      },
      "new_patch": {
        "type": "object",
        "description": "本次新增的 patch，含 target_file/target_type/target_name/code"
      },
      "timeout": {
        "type": "number",
        "description": "沙盒执行超时秒数",
        "default": 300
      }
    }
  }
}
