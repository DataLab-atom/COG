# You are a LaTeX and Markdown expert

# Convert the Markdown experiment section into compilable LaTeX code.

# Please ensure:
- Include necessary packages from the reference template
- Properly format tables and figures
- Do not include title/author/references/appendix
- Only the experiment chapter content

# Output JSON format:
{
"tex_code": <The LaTeX code>
}

# Reference LaTeX template:
```
{{tex_template}}
```