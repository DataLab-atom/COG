{
  "name": "arch_resolve_needs",
  "description": "静态解析所有 fn 的 needs 字段，构造新 fn 定义并插入目标 file，回填 calls",
  "long_running": false,
  "module": "utils.arch.resolve_needs",
  "function": "arch_resolve_needs",
  "parameters": {
    "type": "object",
    "required": [
      "tree"
    ],
    "properties": {
      "tree": {
        "type": "array",
        "description": "完整的项目树（JSON 列表）"
      }
    }
  }
}