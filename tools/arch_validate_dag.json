{
  "name": "arch_validate_dag",
  "description": "对 module imports、file imports、fn calls 进行有向图环检测",
  "long_running": false,
  "module": "arch.validate_dag",
  "function": "arch_validate_dag",
  "parameters": {
    "type": "object",
    "required": [
      "tree",
      "check_targets"
    ],
    "properties": {
      "tree": {
        "type": "array",
        "description": "完整的项目树（JSON 列表）"
      },
      "check_targets": {
        "type": "array",
        "items": {
          "type": "string",
          "enum": [
            "module_imports",
            "file_imports",
            "fn_calls"
          ]
        },
        "description": "需要检查的目标列表"
      }
    }
  }
}