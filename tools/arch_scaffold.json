{
  "name": "arch_scaffold",
  "description": "根据架构树静态生成目录结构和代码骨架（imports + 类定义 + 函数 stub），返回 {file_path: skeleton_code} 映射",
  "long_running": false,
  "module": "arch.scaffold",
  "function": "arch_scaffold",
  "parameters": {
    "type": "object",
    "required": [
      "tree",
      "output_dir"
    ],
    "properties": {
      "tree": {
        "type": "array",
        "description": "完整的架构树（JSON 列表）"
      },
      "output_dir": {
        "type": "string",
        "description": "项目输出根目录路径"
      }
    }
  }
}