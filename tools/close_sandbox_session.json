{
  "name": "close_sandbox_session",
  "description": "关闭共享沙箱会话的输入通道，触发后台会话处理完所有飞行中的请求后清理退出。通道不存在时静默返回 ok=True。",
  "module": "utils.mcts.tools",
  "function": "close_sandbox_session",
  "parameters": {
    "type": "object",
    "required": ["sandbox_in_ch"],
    "properties": {
      "sandbox_in_ch": {
        "type": "string",
        "description": "共享沙箱会话输入通道 ID（由 start_sandbox_session 创建）"
      }
    }
  }
}
