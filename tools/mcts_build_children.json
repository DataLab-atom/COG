{
  "name": "mcts_build_children",
  "description": "将 map 步骤返回的 mcts_single_combo 结果列表转换为子节点 dict，注册到 all_nodes",
  "long_running": false,
  "module": "utils.mcts.tools",
  "function": "mcts_build_children",
  "parameters": {
    "type": "object",
    "required": ["combo_results", "all_nodes", "generation", "best_node"],
    "properties": {
      "combo_results": {
        "type": "array",
        "description": "map 步骤返回的 mcts_single_combo 输出列表"
      },
      "all_nodes": {
        "type": "object",
        "description": "当前 all_nodes 注册表（将被扩展）"
      },
      "generation": {
        "type": "integer",
        "description": "当前代数，写入新节点的 generation 字段"
      },
      "best_node": {
        "type": "object",
        "description": "当前全局最优节点"
      }
    }
  }
}
