{
  "name": "arch_augment_update_patches",
  "description": "loop 辅助：将新生成的 patch 追加到 patches_so_far，返回更新后列表和 has_remaining 条件",
  "module": "utils.arch.design_patch",
  "function": "arch_augment_update_patches",
  "parameters": {
    "type": "object",
    "required": ["patches_so_far", "new_patch", "remaining_items"],
    "additionalProperties": false,
    "properties": {
      "patches_so_far": { "type": "array", "description": "已生成的 patch 列表" },
      "new_patch": { "type": "object", "description": "本轮生成的单个 patch" },
      "remaining_items": { "type": "array", "description": "弹出当前项后的剩余计划项" }
    }
  }
}
