{
  "name": "papw_init",
  "description": "预创建 papw 根目录（{output_dir}/papw），返回 papw_root 路径。用于在代码优化流水线启动的同时并行启动文献下载和向量库构建。",
  "module": "utils.papw_tools",
  "function": "papw_init",
  "parameters": {
    "type": "object",
    "required": ["output_dir"],
    "properties": {
      "output_dir": {
        "type": "string",
        "description": "项目输出根目录，papw/ 子目录将在此创建"
      }
    }
  }
}
