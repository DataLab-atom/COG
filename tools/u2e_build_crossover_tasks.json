{
  "name": "u2e_build_crossover_tasks",
  "description": "将短期反思结果与亲本代码组合为交叉生成任务列表，供下游 map+agent 并发生成新代码",
  "long_running": false,
  "module": "utils.u2e_tools",
  "function": "u2e_build_crossover_tasks",
  "parameters": {
    "type": "object",
    "required": [
      "reflections", "reflect_tasks",
      "user_generator_prompt", "func_signature",
      "long_term_reflection", "func_name"
    ],
    "properties": {
      "reflections": {
        "type": "array",
        "description": "短期反思 LLM 输出列表（与 reflect_tasks 等长）"
      },
      "reflect_tasks": {
        "type": "array",
        "description": "u2e_build_reflect_tasks 产出的 tasks（含 worse_code / better_code）"
      },
      "user_generator_prompt": {
        "type": "string",
        "description": "当前函数的 user_generator 提示词"
      },
      "func_signature": {
        "type": "string",
        "description": "当前函数签名（crossover prompt 中 worse/better 共用）"
      },
      "long_term_reflection": {
        "type": "string",
        "description": "当前长期反思字符串（供参考，不直接嵌入 crossover prompt）"
      },
      "func_name": {
        "type": "string",
        "description": "当前函数名"
      }
    }
  }
}
