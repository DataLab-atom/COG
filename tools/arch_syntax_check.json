{
  "name": "arch_syntax_check",
  "description": "对 arch_assemble 写入磁盘的 Python 文件逐一执行 py_compile 语法检查，返回检查结果和错误列表",
  "module": "utils.arch.arch_validate",
  "function": "arch_syntax_check",
  "parameters": {
    "type": "object",
    "required": ["written"],
    "properties": {
      "written": {
        "type": "array",
        "description": "已写入磁盘的 Python 文件绝对路径列表",
        "items": { "type": "string" }
      }
    }
  }
}
