{
  "name": "arch_collect_fn_bodies",
  "description": "将 map collect:keyed 输出 {fn_id: {body, imports}} 聚合为 arch_assemble 所需的 fn_bodies 和 file_imports",
  "module": "utils.arch.codegen_task_builder",
  "function": "arch_collect_fn_bodies",
  "parameters": {
    "type": "object",
    "required": ["tasks", "keyed_results"],
    "properties": {
      "tasks":           { "type": "array",  "description": "arch_build_codegen_tasks 产出的任务列表" },
      "keyed_results":   { "type": "object", "description": "map collect:keyed 输出 {fn_id: {body, imports}}" },
      "existing_bodies": { "type": "object", "description": "arch_build_codegen_tasks 产出的已有函数体 {fn_id: body}，来自 arch_from_existing，直接合并跳过 LLM" }
    }
  }
}
