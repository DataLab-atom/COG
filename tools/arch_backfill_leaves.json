{
  "name": "arch_backfill_leaves",
  "description": "将 map 步骤逐文件叶子结果回填到文件，设置 kind，汇总为全量类型/函数列表",
  "long_running": false,
  "module": "utils.arch.backfill_map",
  "function": "arch_backfill_leaves",
  "parameters": {
    "type": "object",
    "required": ["files", "leaf_results"],
    "properties": {
      "files": {
        "type": "array",
        "description": "文件列表（来自 file_fanout 步骤）"
      },
      "leaf_results": {
        "type": "array",
        "description": "每个文件对应的叶子结果（map 步骤原始结果，list[dict]）"
      },
      "global_ids": {
        "type": "array",
        "items": { "type": "string" },
        "description": "已有全局 id 列表，默认 []"
      }
    }
  }
}
