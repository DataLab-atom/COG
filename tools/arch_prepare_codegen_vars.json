{
  "name": "arch_prepare_codegen_vars",
  "description": "从任务定义和运行时依赖输出中提取 arch_codegen agent 所需的所有变量",
  "module": "utils.arch.codegen_task_builder",
  "function": "arch_prepare_codegen_vars",
  "parameters": {
    "type": "object",
    "required": ["task", "deps"],
    "properties": {
      "task": { "type": "object", "description": "arch_build_codegen_tasks 产出的单个任务" },
      "deps": { "type": "object", "description": "依赖项输出字典 {fn_id: {body, imports}}，来自 map _deps" },
      "data_files": { "type": "array", "description": "项目中可用的数据文件列表 [{path, size_mb, ext}]，供 main 函数加载真实数据" }
    }
  }
}
