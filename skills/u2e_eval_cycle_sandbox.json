{
  "name": "u2e_eval_cycle_sandbox",
  "description": "U2E 沙盒评估子图（patch 模式）：将含 patches 的 individual 列表并发评估并聚合结果，方向转换：MCTS score → U2E obj = -score",
  "long_running": true,
  "owned_by": "graphs/u2e_eval_cycle_sandbox.json",
  "input_schema": {
    "type": "object",
    "required": ["population", "sandbox_in_ch", "function_evals"],
    "properties": {
      "population":     { "type": "array",   "description": "待评估的 individual 列表（每项含 patches 字段）" },
      "sandbox_in_ch":  { "type": "string",  "description": "共享沙箱会话输入通道 ID" },
      "function_evals": { "type": "integer", "description": "当前累计评估次数（传递给 aggregate 步骤）" }
    }
  },
  "output_schema": {
    "type": "object",
    "required": ["population", "function_evals"],
    "properties": {
      "population":     { "type": "array",   "description": "已打分的 individual 列表（含 exec_success / obj / score）" },
      "function_evals": { "type": "integer", "description": "更新后的累计评估次数" }
    }
  }
}
