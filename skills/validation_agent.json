{
  "name": "validation_agent",
  "description": "从自然语言需求描述中解析优化目标，输出结构化 metric_spec（metric_name + direction）",
  "long_running": false,
  "owned_by": "configs/validation_agent.json",
  "input_schema": {
    "type": "object",
    "required": ["requirement"],
    "properties": {
      "requirement": {
        "type": "string",
        "description": "自然语言描述的项目需求，包含优化目标信息"
      }
    }
  },
  "output_schema": {
    "type": "object",
    "required": ["metric_name", "direction"],
    "properties": {
      "metric_name": {
        "type": "string",
        "description": "标准化指标名称，小写下划线，例如 nmse、per_class_accuracy"
      },
      "direction": {
        "type": "string",
        "enum": ["minimize", "maximize"],
        "description": "优化方向：minimize 表示越小越好，maximize 表示越大越好"
      }
    }
  }
}
